/**
 * @file LedColors.hpp
 * @brief Contains all the colors used by the LedDriver
 */

#include <FastLED.h>

#pragma once

CRGB LedDriver_previewColor = CRGB(0, 255, 0);
CRGB LedDriver_liveColor = CRGB(255, 0, 0);
CRGB LedDriver_offColor = CRGB(0, 0, 0);
